{
	"name": "frontend",
	"version": "0.1.0",
	"scripts": {
		"postinstall": "npm install --prefix Backend && npm install --prefix Frontend && npm run build --prefix Backend"
	},
	"engines": {
		"node": "10.3.0"
	}
}
